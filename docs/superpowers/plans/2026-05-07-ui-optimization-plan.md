# UI 设计优化计划

> **创建时间**：2026-05-07
> **优化范围**：首页、文本对话页、模型配置页、全局样式
> **目标**：渐进式优化，保持现有功能结构，统一设计语言

---

## 1. 首页 (/) 优化

### 1.1 设计目标
- 强化视觉层次和品牌识别
- 添加微动效提升体验
- 优化移动端展示效果

### 1.2 优化内容

#### 1.2.1 Hero 区域
- 添加动态渐变背景
- 标题使用渐变文字效果
- 添加呼吸动画或微光效果

#### 1.2.2 功能卡片
- 添加悬浮光效（hover时光晕效果）
- 优化卡片圆角和边框
- 添加图标动画（hover时微动）
- 优化间距和比例

#### 1.2.3 底部信息
- 优化品牌信息展示
- 添加社交链接（可选）

### 1.3 预期效果
- 视觉冲击力更强
- 交互体验更流畅
- 移动端适配更完善

---

## 2. 文本对话页 (/chat) 优化

### 2.1 设计目标
- 优化消息气泡样式
- 增强加载状态动效
- 提升整体视觉层次

### 2.2 优化内容

#### 2.2.1 消息气泡
- 用户消息：渐变背景或高亮边框
- AI消息：保持纯色但优化圆角
- 添加时间戳和模型标签样式
- 优化附件展示样式

#### 2.2.2 加载状态
- 优化打字机指示器样式
- 添加骨架屏效果（可选）
- 添加微光扫描动画

#### 2.2.3 侧边栏
- 优化会话列表项样式
- 添加悬浮高亮效果
- 优化新建会话按钮

#### 2.2.4 头部区域
- 优化模型选择器样式
- 添加面包屑导航（可选）

---

## 3. 模型配置页 (/config) 优化

### 3.1 设计目标
- 统一设计语言
- 优化表单和卡片样式
- 增强交互反馈

### 3.2 优化内容

#### 3.2.1 模型卡片
- 优化卡片边框和阴影
- 添加悬浮动效
- 统一启用/禁用状态样式

#### 3.2.2 表单样式
- 统一输入框样式
- 添加聚焦状态动效
- 优化标签和占位符

#### 3.2.3 按钮样式
- 统一主次按钮风格
- 添加加载状态样式
- 优化禁用状态

---

## 4. 全局样式优化

### 4.1 CSS 变量
```css
:root {
  /* 主色调 */
  --color-primary: #6366f1;
  --color-primary-dark: #4f46e5;
  --color-primary-light: #818cf8;
  
  /* 次色调 */
  --color-secondary: #ec4899;
  --color-secondary-dark: #db2777;
  
  /* 成功/警告/错误 */
  --color-success: #10b981;
  --color-warning: #f59e0b;
  --color-error: #ef4444;
  
  /* 表面色 */
  --color-surface: #0f0f14;
  --color-surface-light: #1a1a24;
  --color-surface-lighter: #252532;
  
  /* 边框和文本 */
  --color-border: #2d2d3d;
  --color-text: #f1f1f3;
  --color-text-muted: #9ca3af;
  --color-text-dim: #6b7280;
}
```

### 4.2 通用组件样式
- **玻璃态卡片**：添加微光边框效果
- **按钮**：优化圆角、阴影、过渡
- **输入框**：统一样式和动效
- **标签/徽章**：统一风格

### 4.3 动效规范
```css
/* 标准过渡 */
transition: all 0.2s ease-out;

/* 悬浮放大 */
transform: scale(1.02);

/* 点击缩小 */
transform: scale(0.98);

/* 渐入动画 */
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(10px); }
  to { opacity: 1; transform: translateY(0); }
}

/* 脉冲动画 */
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}

/* 微光扫描 */
@keyframes shimmer {
  0% { background-position: -200% 0; }
  100% { background-position: 200% 0; }
}
```

---

## 5. 实施顺序

### 第一阶段：全局基础（优先级：高）
1. 优化 CSS 变量和全局样式
2. 统一玻璃态卡片样式
3. 优化按钮组件样式
4. 添加通用动效类

### 第二阶段：首页优化（优先级：高）
1. Hero 区域视觉增强
2. 功能卡片动效优化
3. 移动端适配

### 第三阶段：对话页优化（优先级：中）
1. 消息气泡样式优化
2. 加载状态动效
3. 侧边栏交互优化

### 第四阶段：配置页优化（优先级：中）
1. 模型卡片样式优化
2. 表单样式统一
3. 按钮样式优化

---

## 6. 验收标准

- [ ] 所有页面视觉风格统一
- [ ] 动效流畅自然，无卡顿
- [ ] 移动端显示正常
- [ ] 交互反馈明确
- [ ] 无视觉突兀的元素
- [ ] 构建无错误

---

## 7. 预期交付物

1. **优化的首页** - 更具视觉冲击力的入口页面
2. **美化的对话页** - 消息展示更清晰，加载动效更流畅
3. **统一的配置页** - 表单和卡片样式与整体风格一致
4. **完善的全剧样式** - 统一的组件样式和动效规范

---

**文档状态**：已创建
**下一步**：开始实施第一阶段（全局基础优化）
